#include <iostream> 
#include <string> 
using namespace std; 
int main() 
{ 
  int a; 
  cout<<"Enter the number"
  cin >> a; 
  int c = a/100; 
  int b = a%100; 
  a = b * 10 + c; 
  cout << "Number = " <<a; 
}
